server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vogellacompany/codeexamples-javaweb/com.vogella.spring.config
          searchPaths: config